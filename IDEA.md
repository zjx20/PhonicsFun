我要做一个web应用，教小孩自然拼读。所谓自然拼读就是一个英语单词，比如“word”，按照字母排布分成3个音节“w-or-d”，掌握每个音节的读音就能拼读出整个单词。这个web应用的核心就是展示单词卡：单词卡有这个单词的基本释义、读音拆解、单词的发音音频，以及拼读的音频——一段连续的音频读出每一个音节，然后读出完整的单词，例如 `/w/ - /ɜː/ - /d/ word`。

这个应用是 AI 原生应用，用户只需要录入待学习的单词，其他内容都由AI生成出来，如单词释义、音标等文本内容，由LLM负责生成；拼读音频则由 live 模型负责生成。这里使用 google aistudio 提供的 gemini-3.5-flash-lite 模型作为 LLM 模型，gemini-3-flash-live 模型作为 live 模型。虽然智能水平不是很高，但是够用，而且免费。

应用后端应该支持缓存，否则每次打开卡片都即时生成的话，延迟上遭不住。用户录入单词也应该有AI加持，比如用户拍一张图片，LLM自动提取图片里的单词。用户随手贴一段文本，可能夹杂着标点符号，也要能智能提取出单词。最终出来一个单词列表，用户通过点击就能剔除不要的单词，不需要通过虚拟键盘编辑。

每天要学习的单词都不一样，可以把单次导入的单词做成一组，展示单词卡的时候在一个组里循环切换展示。

后端用go实现好一点，数据库用sqlite，或者纯文件组织；不用考虑多用户隔离之类的问题，整个应用就是一个单例；我准备把它部署到资源较为拮据的软路由平台上。前端全部自己host，不依赖外部资源。
